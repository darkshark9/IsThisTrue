name = "is-this-true-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

# API key: never commit it. Inject at runtime via Wrangler secrets only.
# After deploying, run: npx wrangler secret put GEMINI_API_KEY
# Then paste your key when prompted. It will be encrypted and stored by Cloudflare.
